#include <stdio.h>

int ft_strcat(char *dest, char *src){
    int i = 0;
    int j = 0;

while (src[i] != '\0')
while (src[j] != '\0')
    {
        i++;
    }

    return (j);

}
//retourne la longueur de la concatenation